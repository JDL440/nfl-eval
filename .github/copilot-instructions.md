Use the custom `squad` agent as the default coordinator for work in this repository.

If a session starts in any other agent and the task involves article production, backlog movement, Ralph orchestration, GitHub issue workflow, or other multi-step repo work, switch to `squad` immediately and continue there without asking first.

Only stay in the current agent when `squad` is unavailable or the task is a tiny one-off action that does not need orchestration.
