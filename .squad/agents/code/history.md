# History — Code

## Project Context

- **Project:** NFL Lab (nfl-eval) — AI-powered NFL analytics and content platform
- **Stack:** TypeScript, Node.js, Hono, HTMX, SQLite, Vitest
- **Owner:** Joe Robinson
- **Repo:** JDL440/nfl-eval
- **Key paths:** `src/` (core), `src/dashboard/` (Hono routes), `src/pipeline/` (article pipeline), `src/llm/` (LLM gateway), `src/mcp/` (MCP tools), `tests/` (vitest), `vitest.config.ts`

## Core Context

- Issue #107 locked the canonical `substack-article` contract: one TLDR/image policy source for Writer, Editor, Publisher, and pipeline guards.
- Issue #108 added the post-Stage-7 retrospective runtime and repository persistence, with structured tables plus a synthesized `revision-retrospective-rN.md` artifact.
- Issue #109 renders revision history from the conversation/revision seams; Issue #110 keeps stage timing as a dashboard aggregation over existing timestamps.
- Dashboard architecture is Hono + HTMX + SSE on port 3456, with config loaded from `.env` and `~/.nfl-lab/config/.env`.
- Issue #115 runtime already satisfied by existing manual/read-only retrospective digest seam: `src/cli.ts` provides `retrospective-digest` / `retro-digest`, `src/db/repository.ts` reads joined structured retrospective rows, `src/pipeline/actions.ts` persists findings, and operator guidance is documented in `README.md`.
- Issue #117 keeps the retrospective digest CLI read-only, using one joined repository query plus normalized-text dedupe and bounded markdown/JSON output.
- Issue #118 promotion rule: repeated `process_improvement` findings must promote to issue-ready independent of author or priority. Added explicit repetition check to `promoteIssueCandidates()`.
- Issue #120 stores revision blocker metadata directly on `revision_summaries` via `blocker_type` + JSON `blocker_ids`, with backward-compatible handoff through `feedback_summary` and `key_issues`. Write seam: `src/pipeline/actions.ts` (parses `[BLOCKER type:id]` tags from `## 🔴 ERRORS`); read seam: `src/pipeline/conversation.ts` + `src/db/repository.ts`.
- Issue #123 repeated-blocker escalation at Stage 6: detects exact consecutive Editor `REVISE` comparison using normalized blocker fingerprint (`blockerType` lowercase + `blockerIds` sorted/deduped), writes `lead-review.md`, sets status to `needs_lead_review`, blocks normal regress/force-approve path only for repeated case. Cleanup via `clearArtifactsAfterStage` on regression below Stage 6. Test coverage: 5 focused tests in actions.test.ts, 2 in conversation.test.ts, 2 in repository.test.ts, 1 in server.test.ts. Approved and ready for merge.
- Issue #125 writer fact-check: three-slice arc complete (Policy Definition → Runtime Enforcement → Editor Consumption). Slice A: bounded policy in `src/pipeline/writer-factcheck.ts`. Slice B: Stage 5 wall-clock budget enforcement via fetch-level abort signal. Slice C: Editor consumption of `writer-factcheck.md` as advisory context in `src/pipeline/context-config.ts` + `src/pipeline/actions.ts`. Writer provides bounded verification ledger; Editor maintains final authority. Validation passed.
- Optional dashboard services (Substack, Twitter) resolve through startup dependency injection via `resolveDashboardDependencies()` helper in `src/dashboard/server.ts`. Tests exercise real startup path; env-configured services work correctly.
- Writer revision retry: `writeDraft()` self-heal failures append failed draft under `## Failed Draft To Revise` for revision instead of restarting. Prompt guidance aligned across `src/config/defaults/charters/nfl/{editor,publisher}.md` plus `src/config/defaults/skills/{editor-review,publisher}.md`. TLDR fixes framed as revisions that preserve working analysis.
- Writer revision handoff is assembled in `src/pipeline/actions.ts` inside `writeDraft()`: shared cross-role context from `buildRevisionSummaryContext()`, full `editor-review.md` and previous `draft.md` injected into `articleContext`. Merged writer prompt is runtime-only in `src/agents/runner.ts`; SQLite persists pieces (`artifacts`, `article_conversations`, `revision_summaries`) but not canonical per-iteration prompt snapshot.
- Publish payload: HTML→ProseMirror handling refactored to operate on document nodes instead of string replacement. All validation passed (45 passing tests).
- `buildRevisionHistoryEntries()` must normalize missing legacy/in-memory `blocker_type` values to `null` before dashboard/API consumers render revision history.
- Stage 5 context seam for new artifacts is `src/pipeline/context-config.ts`: low-risk way to make artifacts available to Writer without widening Editor/Publisher scope.

## Learnings

- 2026-03-25T03:32:04Z — **UX Dashboard Mobile System Audit Handoff**: UX completed comprehensive system-level mobile audit and locked mobile contract recommendations. Key findings align with Code observations: shell has no responsive nav pattern, grids collapse but shell/hierarchy don't, action surfaces lack mobile stacking contract, data surfaces only have horizontal scroll fallback, HTMX fragments bypass mobile context, selector collisions (.agent-grid used twice), and test coverage missing mobile structural assertions. UX decision documented in `.squad/decisions.md`: defines shared mobile shell behavior, three responsive primitives (action-stack, data-surface cards, secondary-panel collapse), and HTMX/SSE compliance rules. Implementation sequence split UX (days 1–2 pattern lock, selector map) → Code (days 3–7 shell in `layout.ts`, CSS system in `styles.css`, page group migration, testing). Orchestration: `.squad/orchestration-log/2026-03-25T03-32-04Z-ux.md`. Session: `.squad/log/2026-03-25T03-32-04Z-dashboard-mobile-audit.md`.
- 2026-03-27T20:35:00Z — **Dashboard mobile audit seam**:shared mobile issues are systemic, not page-local. `src/dashboard/views/layout.ts` still renders a one-row header/nav shell, while `src/dashboard/public/styles.css` has no dedicated `.header-nav` mobile treatment and no CSS at all for the newer layout hooks already present in `src/dashboard/views/{article,publish}.ts` (`mobile-detail-layout`, `mobile-primary-column`, `mobile-secondary-column`, `article-detail-layout`, `publish-detail-layout`). Data-heavy pages also diverge on fallback strategy instead of sharing one mobile surface: `/runs` uses overflow-only table wrap (`src/dashboard/views/runs.ts` + `.runs-table-wrap`), `/memory` keeps a raw table with slight font shrink (`src/dashboard/views/memory.ts` + `.memory-table`), and `/config` renders bare `.artifact-table` sections with no wrapper (`src/dashboard/views/config.ts`). Mobile fixes should land as shared shell/CSS/view primitives in `src/dashboard/public/styles.css` + the shared view partials, not as one-off page patches. Tests currently cover content and route behavior but not responsive behavior (`tests/dashboard/{server,new-idea,publish,runs,wave2}.test.ts`); focused dashboard run on 2026-03-27 showed 202/204 passing with two pre-existing auto-advance failures in `tests/dashboard/new-idea.test.ts`, unrelated to mobile.
- 2026-03-27T20:50:00Z — **Dashboard mobile system audit refresh**: current strongest mobile evidence is that shared mobile hooks now exist in markup/tests but still do not drive CSS. `src/dashboard/views/layout.ts`, `src/dashboard/views/article.ts`, and `src/dashboard/views/publish.ts` emit `shared-mobile-header`, `shared-mobile-nav`, `mobile-detail-layout`, `mobile-primary-column`, `mobile-secondary-column`, and `publish-workflow-actions`, and tests in `tests/dashboard/{server,publish,runs,new-idea}.test.ts` assert those strings, but `src/dashboard/public/styles.css` still has no selectors for those hook classes. Remaining system risk is concentrated in shared shell/nav behavior, table/data-surface responsiveness (`artifact-table`, `memory-table`, `runs-table-wrap`), and toolbar/filter stacking (`filter-bar`, `preview-toolbar`, `composer-meta`, `send-back-form`) rather than in route wiring.
- 2026-03-27T22:10:00Z — **Dashboard mobile test audit refresh**: current mobile protection is stronger on markup hooks than on CSS behavior. `tests/dashboard/server.test.ts`, `tests/dashboard/runs.test.ts`, and `tests/dashboard/publish.test.ts` now lock shared/mobile intent classes from `src/dashboard/views/{layout,article,publish}.ts` (`shared-mobile-header`, `shared-mobile-nav`, `mobile-detail-layout`, `mobile-primary-column`, `mobile-secondary-column`, `publish-workflow-actions`) plus primary-before-secondary DOM order, but `src/dashboard/public/styles.css` still has no matching selector rules for those hook classes, so the suite protects structural intent rather than actual narrow-screen rendering. Real mobile behavior remains mostly tied to older shared selectors like `.dashboard-grid`, `.detail-grid`, `.filter-bar`, `.runs-table-wrap`, `.memory-filter-bar`, `.artifact-table`, `.memory-table`, `.team-grid`, and `.preview-container.preview-mobile`; focused dashboard validation currently sits at 233 passing / 2 failing with only `tests/dashboard/new-idea.test.ts` auto-advance regressions unrelated to mobile.
- 2026-03-27T20:35:00Z — **Dashboard mobile regression hooks landed**: mobile dashboard tests are most stable when they assert semantic structure hooks instead of desktop markup shape. Shared shell coverage now targets `shared-mobile-header` / `shared-mobile-nav`, article and publish coverage uses primary-first column hooks (`mobile-primary-column` / `mobile-secondary-column`), and New Idea expert pinning is isolated on `.idea-agent-grid` so it no longer collides with the agents directory grid.
- 2026-03-27T14:00:00Z — **Dashboard Mobile System Audit Complete**: The dashboard mobile system has 6 critical shared failures (grid collapse at 769px, `.agent-grid` naming collision, filter bar doesn't stack, team grid too wide on mobile, stage timeline horizontal-only, data tables lack mobile view). All failures are in `src/dashboard/public/styles.css` and can be fixed via 5 CSS breakpoint edits + 1 HTML rename in `src/dashboard/views/new-idea.ts`. Minimum viable mobile fix: (1) add `@media (max-width: 640px)` rule to `.dashboard-grid` to collapse 2-col → 1-col, (2) rename `.agent-grid` → `.expert-agent-grid` in new-idea.ts to eliminate CSS cascade collision, (3) add stacking rule to `.filter-bar`, (4) add mobile breakpoint to `.team-grid` (8-col → 4-col), (5) add vertical fallback to `.stage-timeline`. Total effort ~40 min, covers 6+ pages. Current tests are route/render-focused; mobile responsive assertions missing from all suites. Full audit written to `.squad/agents/code/mobile-system-audit.md`. See `.squad/decisions/inbox/code-mobile-audit.md` for implementation sequence.
- 2026-03-25T12:15:00Z — **Dashboard mobile rework seam audit**: the safest dashboard-wide mobile pass is to work from shared CSS/layout seams outward, not page-by-page. Highest-leverage/shared-risk surfaces are `src/dashboard/views/layout.ts` + `src/dashboard/public/styles.css`, especially the shared `detail-grid`, `filter-bar`, table wrappers (`runs`, `memory`, `config`), and preview/publish container patterns. Biggest hidden coupling: `.agent-grid` is used by both `src/dashboard/views/new-idea.ts` and `src/dashboard/views/agents.ts` with conflicting intent, so that class should be isolated or renamed before broad mobile tweaks. Current dashboard tests (`tests/dashboard/{server,new-idea,publish,runs,wave2}.test.ts`) mostly protect route/render behavior, not responsive layout, so any mobile pass needs targeted assertions for stacked grids, filters, and narrow-screen data presentation.
- 2026-03-25T03:29:13Z — **Dashboard Mobile Audit Session Record Complete**: Scribe completed orchestration and session logs for the dashboard mobile audit. Code decision merged to `decisions.md`: treat dashboard mobile work as shared-system rollout in this order: (1) shared shell/navigation contract, (2) shared responsive data-surface contract, (3) shared detail/preview stacking contract, (4) page-specific selector-density cleanup, (5) targeted mobile regression coverage. Minimum implementation split: UX owns shell/data-surface contract definition; Code owns shared seams in `layout.ts` + `styles.css` plus page group migration. Tests added only after shared contract exists. Inbox decisions merged and archived. See `.squad/orchestration-log/2026-03-25T03-29-13Z-mobile-audit-tests-fast.md` and `.squad/log/2026-03-25T03-29-13Z-dashboard-mobile-audit.md`.
- 2026-03-24T22:20:00Z — **V3 Stage 5/6/7 simplification slice**: keep prompt-policy ownership narrow and role charters lean. `src/config/defaults/skills/substack-article.md` is the canonical source for article structure / TLDR / image contract, `src/config/defaults/skills/writer-fact-check.md` remains the canonical Stage 5 bounded verification policy, and `src/config/defaults/skills/editor-review.md` is the canonical Stage 6 review + verdict protocol. Runtime follow-through lives in `src/pipeline/context-config.ts` (reduced default context weight for `writeDraft`, `runEditor`, `runPublisherPass`) and `src/pipeline/actions.ts` (Writer task points at the canonical structure contract; Editor runtime avoids duplicate `roster-context.md` / `fact-check-context.md` injection when already present via config). Charters in `src/config/defaults/charters/nfl/{writer,editor,publisher}.md` should stay focused on role/voice/boundaries rather than re-copying policy. Focused validation command: `npm test -- --run tests/pipeline/actions.test.ts tests/pipeline/writer-factcheck.test.ts` → 83/83 passing in the V3 worktree.
- 2026-03-27T00:24:30Z — **Writer preflight artifact seam finalized**: `src/pipeline/writer-preflight.ts` now owns both the shared editor-style checklist text and the persisted `writer-preflight.md` artifact format (`WRITER_PREFLIGHT_ARTIFACT_NAME` + `buildWriterPreflightArtifact()`), while `src/pipeline/actions.ts` only records initial/final preflight state and whether the retry path was triggered. Focused regression coverage that locks the runtime behavior lives in `tests/pipeline/actions.test.ts`: prompt injection should assert against the writer user message, clean first-pass drafts should still persist `writer-preflight.md`, and deterministic repair-path cases should assert both the retry instruction and the persisted artifact summary. Validation passed with `npm run v2:test -- tests/pipeline/actions.test.ts tests/pipeline/engine.test.ts tests/pipeline/writer-factcheck.test.ts tests/agents/runner.test.ts` and `npm run v2:build`.
- 2026-03-25T00:18:00Z — **Writer/Editor preflight landed**: Keep Writer/Editor alignment in the Stage 5 seam, not the generic runner. Implementation uses `src/pipeline/writer-preflight.ts` for a short editor-style checklist plus a bounded deterministic linter (name consistency / unsupported full-name expansion / unsupported precise contract-date-stat language from supplied artifacts only), and `src/pipeline/actions.ts` consumes it inside `writeDraft()` before Editor with the existing one-retry repair path. Focused regression coverage lives in `tests/pipeline/actions.test.ts` and `tests/pipeline/writer-preflight.test.ts`; broader safety net validated with `tests/pipeline/{actions,writer-preflight,engine,writer-factcheck}.test.ts` plus `npm run v2:build`.
- 2026-03-24T23:26:00Z — **Writer/Editor alignment seam mapped**: `src/pipeline/actions.ts` owns the full Stage 5 path today: Lead preflight (`panel-factcheck.md`), bounded Writer contract artifact (`writer-factcheck.md` via `src/pipeline/writer-factcheck.ts`), Writer prompt/task assembly, and the existing one-retry structural self-heal. `src/agents/runner.ts` is intentionally generic prompt assembly; the lowest-risk high-value fix is a shared Stage 5 helper consumed from `writeDraft()` rather than broad runner specialization. Best validation surface is `tests/pipeline/actions.test.ts` plus `tests/pipeline/engine.test.ts` and, if a new helper is extracted, a focused pipeline unit like `tests/pipeline/writer-preflight.test.ts`. Baseline validation passed with `npm run v2:test -- tests/pipeline/actions.test.ts tests/pipeline/engine.test.ts tests/agents/runner.test.ts tests/pipeline/writer-factcheck.test.ts` and `npm run v2:build`.
- 2026-03-23T21:53:55Z — **Issue #102 Auth Flow Rundown Complete**: Quick technical summary of auth implementation provided. Config loading via `DASHBOARD_AUTH_MODE` from `.env` + `~/.nfl-lab/config/.env`, login/logout via POST routes, session persistence in SQLite with opaque ids, secure cookie settings (httpOnly, SameSite, Secure), centralized middleware enforcement of protected/public route split. All test files documented and passing. Ready for merge. See `.squad/orchestration-log/20260323T215355Z-code.md` and session log `.squad/log/20260323T215355Z-issue-102-auth-review-rundown.md`.
- 2026-03-24T04:46:45Z — **Issue #102 Local Dashboard Auth Implementation Complete**: Config-driven `DASHBOARD_AUTH_MODE=off|local` auth hardening delivered and validated. Implementation seams: config parsing in `src/config/index.ts`, login/logout routes + centralized middleware in `src/dashboard/server.ts`, session persistence in `src/db/schema.sql` + `src/db/repository.ts`, middleware enforcement of protected/public routes. Secure defaults applied: opaque session ids, `httpOnly` + `SameSite=Lax` cookies with production `Secure` flag, server-side TTL (24h default). All focused auth tests passing across `tests/dashboard/server.test.ts`, `tests/dashboard/config.test.ts`, `tests/dashboard/publish.test.ts`, `tests/e2e/live-server.test.ts`. TypeScript build failure fixed. Ready for merge pending operator docs. See `.squad/orchestration-log/2026-03-24T04-46-45Z-code.md` and `.squad/decisions.md` (Code Decision — Issue #102).
- 2026-03-25T21:45:00Z — **Issue #123 Closeout & Approval**: Repeated-blocker escalation implementation for Stage 6 handoff completed and approved by Lead. All contract points validated: exact consecutive Editor `REVISE` comparison with normalized blocker fingerprint, repeated case escalates at Stage 6 without new stage, normal loop bypass narrow (skips auto-regress and max-revision force-approve), read paths expose state/artifact appropriately, artifact lifecycle bounded. All focused tests passed. Build passed. Issue ready for merge. See `.squad/orchestration-log/2026-03-24T03-26-23Z-Code.md`.
- 2026-03-24T00:00:00Z — Generate-idea selector seams: the visible team picker on `/ideas/new` is the static `NFL_TEAMS` list in `src/dashboard/views/new-idea.ts`, while the pinned-agent picker is built server-side in `src/dashboard/server.ts` from `runner.listAgents()`. The league-wide charter key is lowercase `nfl` in runtime/team-classification code (`src/dashboard/views/agents.ts`, `src/pipeline/actions.ts`), but the UI team badge uses uppercase `NFL`; keeping those two identifiers aligned is the key to making the NFL-wide agent selectable without surfacing all 32 team charters in the expert picker.
- 2026-03-26T22:39:00Z — **Generate-idea selector seam**: `/ideas/new` uses two different sources. Team-like choices come from the static `NFL_TEAMS` array in `src/dashboard/views/new-idea.ts`, while the optional pinned-expert picker is built in `src/dashboard/server.ts` from `runner.listAgents()` (`config.chartersDir`) after filtering production/team agents. League-wide NFL support should use UI key `NFL` for article/team context and agent key `nfl` for charter/runner classification; keep both in sync across `src/dashboard/server.ts`, `src/pipeline/actions.ts`, `src/dashboard/views/agents.ts`, and `tests/dashboard/{new-idea,agents}.test.ts`.
- 2026-03-26T22:46:00Z — **Stage Runs badge semantics**: \stage_runs.stage\ is already the persisted article/dashboard stage, not a "next stage" target. Keep \enderStageRunsPanel()\ aligned with \rticle.current_stage\ semantics by rendering the stored stage number/name directly; lock that expectation in \	ests/dashboard/wave2.test.ts\ and \	ests/db/repository.test.ts\. Key seams: \src/dashboard/views/article.ts\, \src/db/repository.ts\, \src/types.ts\.
- 2026-03-23T22:44:04Z — **Stage Badge Mismatch Fix Complete**: Fixed dashboard Stage Runs badge display. Article header badge (\rticle.current_stage\) and Stage Runs panel (\stage + 1\ pre-fix) showed different stage numbers for the same article. Narrow fix in \src/dashboard/views/article.ts\ removes the \+ 1\ increment; Stage Runs now renders persisted \stage_runs.stage\ directly. Updated tests in \	ests/dashboard/wave2.test.ts\ (Stage 5 expectation) and \	ests/db/repository.test.ts\ (regression test). All tests passing, build passing. Decision merged to \.squad/decisions.md\. See \.squad/orchestration-log/2026-03-23T22-44-04Z-code.md\.

## 2026-03-25T03:30:39Z — Dashboard Mobile Audit Session (Code Composition Pass)

**Orchestration log:** `.squad/orchestration-log/2026-03-25T03-30-39Z-code.md`  
**Session log:** `.squad/log/2026-03-25T03-30-39Z-dashboard-mobile-audit.md`

**Status:** ⏳ In progress — HTMX/view/server/test composition pass (awaiting response)

---

## Stage 5/6/7 Simplification Run — Prep Decisions (2026-03-24T22:09:06Z)

**Status:** All Code + Lead decisions merged to `.squad/decisions.md`; inbox cleared

**Decision Summary:**
1. **Writer Runtime / Editor Alignment Plan** — Stage 5 seam: editor-aligned checklist + deterministic linter in `writeDraft()` flow
2. **Stage Runs badge semantics** — Render persisted `stage_runs.stage` directly without increment
3. **Writer Preflight Artifact Ownership** — Policy + format localized to `src/pipeline/writer-preflight.ts`
4. **Lead Writer Preflight Alignment** — Confirms seam preference and scope guard

**Key blocker set for deterministic Stage 5 linter:**
- Canonical TLDR contract still missing / too low / too short
- Missing `**Next from the panel:**` ending hook
- Obvious draft leakage (TODO, TBD, placeholder text, editorial notes)
- Direct-quote discipline
- Unsupported superlatives / absolutes

**Next:** Backend implementation of writer-preflight runtime in `src/pipeline/actions.ts` / `src/pipeline/writer-preflight.ts` with deterministic linter in `src/pipeline/engine.ts`.

- 2026-03-24T22:10:02Z — **V3 Stage 1 Dashboard Audit — Server Slice Complete**: Code reviewed Stage 1 server/test slice for legacy route cleanup and contract alignment. Findings: `/htmx/recent-ideas` route still exposed in `src/dashboard/server.ts`; test assertions in `tests/e2e/live-server.test.ts` still reference legacy path; `depthLevel` parameter accepts 4 while UI contract enforces 1-3 simplified levels. Initial test-failure blocking claim was invalidated by coordinator (93/93 passing in direct run). All paths examined: `src/dashboard/server.ts`, `src/dashboard/views/home.ts`, `src/dashboard/views/new-idea.ts`, `src/dashboard/views/article.ts`, `src/dashboard/views/publish.ts`. Session log: `.squad/log/2026-03-24T22-10-02Z-dashboard-audit.md`. Orchestration: `.squad/orchestration-log/2026-03-24T22-10-02Z-code.md`.

---

## Generate Idea Selector — Implementation (2026-03-24T05:37:47Z)

**Status:** Filesystem verified; pending lead approval

**UX confirmed:** Clean render path, no UX gaps. Expert selector correctly filters NFL-wide specialists from production and team agents.

**Changes verified:**
- src/dashboard/views/new-idea.ts (selector integration)
- src/dashboard/server.ts (expert agents filter, lines 847–861)
- tests/dashboard/new-idea.test.ts (selector tests)

**Next:** Implement hygiene fixes (DRY, abbreviation) pending Lead approval.

- 2026-03-24T05:41:29Z — **Generate-Idea Selector Implementation Complete**: Minimal support for league-wide NFL team selection delivered and validated. Changes: `nfl` team classification support in `src/dashboard/views/agents.ts`, `NFL` team grid option in `src/dashboard/views/new-idea.ts`, expert picker server filter update in `src/dashboard/server.ts` to include `nfl` charter. Validation: ✅ `npm run v2:build` passed, ✅ `npm run v2:test -- tests/dashboard/agents.test.ts` passed, ✅ focused new-idea NFL selector tests passed, ⚠️ 2 pre-existing auto-advance failures in `tests/dashboard/new-idea.test.ts` unrelated to selector. **Key correction:** Current workspace allows runtime `nfl` charter to remain selectable in expert pins (not filtered out); change scope is adding/selecting `NFL` in team grid + handling team-classification. Decision documented: Keep existing team-agent filter but treat `nfl` charter as selectable exception. See `.squad/orchestration-log/2026-03-24T05-41-29Z-code.md` and `.squad/decisions.md` (Code Decision — Generate-Idea NFL Selector Support).
- 2026-03-24T05:45:02Z — **Stage Runs Panel Stage Number Alignment Complete**: Fixed Stage Runs badge to render persisted `stage_runs.stage` directly without transformation. Changes in `src/dashboard/views/article.ts` and tests in `tests/dashboard/wave2.test.ts` + `tests/db/repository.test.ts`. All focused tests passing; v2 build passing. Key learning: `stage_runs.stage` is the persisted article/dashboard stage (not a "next stage" target); render semantics must align with `article.current_stage`. See `.squad/orchestration-log/2026-03-24T05-45-02Z-code.md`.
- 2026-03-25T00:35:00Z — **Dashboard mobile system audit**: Audit treated dashboard mobile as one shared system, not page silos. Highest-risk seams are the shared shell in `src/dashboard/views/layout.ts` + `src/dashboard/public/styles.css`, the two-column detail shell in `src/dashboard/views/article.ts` / `src/dashboard/views/publish.ts`, preview toolbar/toggle in `src/dashboard/views/preview.ts`, and dense data surfaces in `src/dashboard/views/{runs,memory,config}.ts`. Key findings: `layout.ts` renders `.header-nav` but `styles.css` has no `.header-nav` rule or mobile nav pattern; `.detail-grid` stacks at `768px` but many child patterns still assume desktop rows (`.card-actions`, `.quick-actions`, `.preview-toolbar`, `.usage-summary`); `.artifact-table` and `.memory-table` have no shared horizontal wrapper/card fallback; `.agent-grid` is overloaded across `new-idea.ts` and `agents.ts`, so mobile CSS changes can drift across screens. Current tests in `tests/dashboard/{server,new-idea,publish,runs,wave2}.test.ts` mostly assert strings/routes/HTMX flows and do not lock viewport-specific structure or responsive classes; focused dashboard suite currently has 2 unrelated auto-advance failures in `tests/dashboard/new-idea.test.ts`, with no mobile-specific failures because no mobile assertions exist.


- 2026-03-25T03:29:17Z — **Dashboard Mobile Audit (Read-Only, Merged)**: Code conducted parallel read-only audit of dashboard mobile system. Findings converged with UX audit: mobile work is system-wide, not page-by-page. Server-side render contract lacks explicit mobile-aware wrappers; shared CSS primitives missing for stacking tables/filters/action-groups; HTMX fragments inherit parent mobile behavior unpredictably; class scoping conflicts (.agent-grid overloaded); test infrastructure lacks viewport/breakpoint assertions. Recommendation: treat as shared-system rollout with explicit phase sequencing (shell → CSS primitives → HTMX fragments → page follow-through). Implementation ownership: UX defines shell/data-surface contracts (days 1–2), Code implements shared seams in layout.ts + styles.css plus page migration (days 3–7). Key artifacts: .squad/orchestration-log/2026-03-25T03-29-17Z-code.md, .squad/log/2026-03-25T03-29-17Z-dashboard-mobile-audit.md, .squad/decisions.md ("Dashboard Mobile Audit — Shared System Approach").
