# Session Log — Issue #110 Triage

Recorded the issue #110 triage pass, captured that article timing totals are already derivable from existing `stage_runs` rows, and noted the separate persistence boundary for any retry-aware or per-state timing split.
