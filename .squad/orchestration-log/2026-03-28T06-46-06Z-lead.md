# Orchestration — Lead Agent

**Session:** 2026-03-28 Second-Pass Workflow Fix  
**Agent:** lead-second-pass-guardrails  
**Status:** ✓ Completed  

## Mission

Validate Code's second-pass fix direction and establish explicit guardrails for Stage 6 advisory/blockerless revise downgrade.

## Decision

The live Seahawks JSN article demonstrates that the remaining stall is **post-approval advisory churn**, not a hard Stage 5 guard failure.

**Evidence:**
- `editor-review.md` (initial): mixed true blockers with suggestions
- `editor-review-2.md` (pass 2): all blockers fixed; verdict `APPROVED`
- `editor-review-3.md` (pass 3): third pass exists only to mop up yellow items; adds HTML TODO placeholder to draft

**Analysis:**
- Article already cleared blocking factual review
- Kept looping through extra cleanup passes for non-blocking items
- Workflow behaves as if yellow suggestions are quasi-required even after Editor approval

## Approved Direction

Runtime normalization seam in `worktrees\V3\src\pipeline\actions.ts`:
1. If Editor returns `REVISE` with no `[BLOCKER type:id]` lines, force one blocker-only normalization pass
2. If still no canonical blocker after retry, treat as `APPROVED` instead of looping

## Protected Guardrails

Code must preserve all of the following:

1. **Minimal Stage 5 shell stays hard**
   - headline, italic subtitle, recognizable TLDR, empty/very short draft guard

2. **Placeholder leakage stays hard**
   - visible TODO/TBD/TK scaffolding must not silently pass

3. **Warnings remain advisory**
   - Stage 5 warnings must not block advancement on their own

4. **Writer revises in place**
   - revision flow must continue to patch existing draft, not restart from scratch

5. **Stage 6 approval semantics stay strict**
   - only true `REVISE`/`REJECT` blockers reopen the loop
   - `APPROVED` must behave as terminal for required review flow

6. **Escalation machinery stays intact**
   - repeated blocker fingerprinting, Stage 6 hold / `needs_lead_review`, no reintroduction of force-pass

7. **Editor remains an accuracy gate**
   - wrong name/team, unsupported exact stat/figure, stale/contradicted claim, fabricated quote/attribution

## Reviewer / Rollback Risks

Reject or roll back if any of these appear:

1. Minimal shell guard weakened (malformed drafts reach Editor without headline/subtitle/TLDR)
2. Placeholder leakage becomes publish-safe (TODO in draft.md, artifacts, or rendered output)
3. Approved no longer means done (`APPROVED` still triggers another required revision)
4. Editor blocker taxonomy widens (structure/style/tone suggestions come back as blocking)
5. Escalation contract breaks (true `REVISE` no longer regresses to Stage 4)
6. Advisory findings become hidden blockers (yellow suggestions still mandatory by publisher/runtime)

## Deliverables

- ✓ Direction validated: second-pass normalization seam approved
- ✓ Guardrails explicit: Code must preserve minimal Stage 5, placeholder hard guard, approval terminal, escalation intact
- ✓ Rollback triggers defined for Code and Reviewer

## Sign-off

- Code: diagnosis and implementation direction ready
- Research: Stage 6 issue class confirmed as evidence-deficit/editor churn

