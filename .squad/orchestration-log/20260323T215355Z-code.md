# Orchestration Log — Code (auth-rundown-102)

**Agent ID:** auth-rundown-102  
**Date:** 2026-03-23T21:53:55Z  
**Mode:** Sync  
**Session:** Issue #102 auth hardening — auth flow rundown

## Task

Quick runtime explanation of the new dashboard auth flow implementation for Issue #102. Summarize:
- Config loading and mode selection
- Login/logout flow and session handling
- Public vs protected route enforcement
- Cookie and session security
- Test/dev behavior specifics

## Summary

### Config Loading
- Reads `DASHBOARD_AUTH_MODE` from `.env` or `~/.nfl-lab/config/.env`
- Modes: `off` (no auth), `local` (single-operator login)
- Env-backed username/password via `DASHBOARD_USERNAME` / `DASHBOARD_PASSWORD`

### Login & Session Handling
- Login route: POST `/login` with credentials
- Session creation: Opaque session id, stored in SQLite `dashboard_sessions`
- Cookie: `httpOnly`, `SameSite=Lax`, production `Secure` flag
- Logout: POST `/logout` clears session

### Route Protection
- **Protected:** Dashboard HTML, HTMX endpoints, JSON API, SSE, unpublished images
- **Public:** Static assets (`/static/*`), login/logout routes, published images
- Enforcement via centralized middleware

### Test Coverage
- `tests/dashboard/server.test.ts` — middleware and route protection
- `tests/dashboard/config.test.ts` — config parsing
- `tests/dashboard/publish.test.ts` — integration with publish flow
- `tests/e2e/live-server.test.ts` — end-to-end auth flow

### Dev/Test Behavior
- Test mode bypasses auth checks for isolated unit testing
- Live server tests exercise real bootstrap path
- Env-configured services work correctly in test environment

## Key Technical Decisions

1. **Minimal design:** Single-operator login, no multi-user/RBAC
2. **Middleware-first:** All route protection via one entry point
3. **SQLite persistence:** Session state survives restarts
4. **Secure defaults:** Production-ready cookie settings by default
