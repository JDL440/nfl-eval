# Orchestration — Publisher

**Timestamp:** 2026-03-23T04:18:42Z
**Session:** Scribe background coordination

## Outcome

Confirmed the dashboard publish failure is caused by missing `SubstackService` wiring at startup, not by missing operator credentials when the service is already configured.

## Key Evidence

- `createApp()` accepts an optional `substackService` dependency.
- Normal `startServer()` startup does not construct or pass `SubstackService`.
- Local article publishing still depends on `SUBSTACK_TOKEN` and `SUBSTACK_PUBLICATION_URL`.
