# Orchestration — Code

**Timestamp:** 2026-03-23T04:18:42Z
**Session:** Scribe background coordination

## Outcome

Traced the server-side 500 path and confirmed the startup wiring gap: `createApp()` can receive `substackService`, but `startServer()` never instantiates or forwards it, so the route falls back to the generic "not configured" response.

## Key Evidence

- `src/dashboard/server.ts` publishes the draft/publish routes behind an optional service dependency.
- The missing dependency is the normal startup path, not the test path.
- Route tests pass because they inject a mock `substackService` directly.
