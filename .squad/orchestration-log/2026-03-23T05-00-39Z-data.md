# Orchestration Log — Data Agent

**Timestamp:** 2026-03-23T05:00:39Z  
**Agent:** Data  
**Scope:** Publish Payload Path Validation

## Work Summary

Identified and documented root cause of missing Substack payload parity: `src/dashboard/server.ts` line 276 was sending raw ProseMirror JSON instead of rendered HTML to Substack API.

### Key Finding

**Bug:** `substackBody = JSON.stringify(doc)` (incorrect)  
**Fix:** `substackBody = proseMirrorToHtml(doc)` (correct — matches preview)

### Error Precedence Validation

Confirmed POST `/api/articles/:id/publish` route checks in correct order:
1. Missing markdown (`!presentation.substackBody`) — "No article draft found yet..."
2. Missing draft URL (`!article.substack_draft_url`) — "No linked Substack draft found..."

### Testing

- ✅ All 42 publish tests pass (`npm run test -- tests/dashboard/publish.test.ts`)
- ✅ Build succeeds (`npm run v2:build`)

## Outcome

Decision **Data — Fix Substack Publish Payload Format** merged to decisions.md. Fix shipped on main.

## Cross-Agent Impact

- **Publisher:** Updated history.md with critical bug note and payload path analysis
- **Code:** Implementation complete, tests pass
