# Orchestration Log — Code

**Date:** 2026-03-25  
**Time:** 10:30:00Z  
**Scope:** Investigate and fix Tweet publishing 500s  

## Current Status

### Dashboard Substack Service Wiring Fixed
- ✅ `startServer()` now initializes `SubstackService` (matched imageService pattern)
- ✅ Routes at `/api/articles/:id/draft` and `/api/articles/:id/publish` receive service via `createApp()`
- ✅ Non-fatal pattern: log warning if env missing, don't crash startup
- ✅ Preserves explicit dependency precedence in tests while fixing production wiring

### Payload Structure Fixes Integrated
- ✅ ProseMirror JSON document format restored (from HTML regression)
- ✅ `buildPublishPresentation()` returns `substackDoc: ProseMirrorDoc` (not string)
- ✅ Node helper functions: `buildImageNode()`, `buildSubscribeNode()`, `buildBlurbNode()`, `buildHorizontalRule()`
- ✅ All 45 publish tests pass; regression test coverage confirms parity

### Active Investigation
**Task:** Investigate Tweet publishing 500s  
**Endpoint:** `/api/articles/:id/tweet` (route handler location pending)  

**Expected steps:**
1. Locate Tweet publish route handler in `src/dashboard/server.ts`
2. Review request/response handling and error patterns
3. Check payload structure for Tweet API expectations
4. Validate against MCP tool contract (if using `nfl-eval-local-publish_tweet`)
5. Cross-reference with image attachment or link generation logic
6. Test Tweet creation with sample data/mock environment

**Related context:**
- Article publish (Substack) now working with ProseMirror JSON
- Tweet payload is independent API (Twitter/X vs Substack)
- Recent changes to image handling may affect Tweet image attachment flow
- See Code history for past Tweet-related issues

## Next: Tweet Publishing Fix
Once Tweet 500 root cause identified, apply targeted fix and comprehensive test coverage matching publish payload repair workflow.
