# Orchestration Log — Publisher Agent

**Timestamp:** 2026-03-23T05:00:39Z  
**Agent:** Publisher  
**Scope:** Substack Payload Parity Restoration

## Work Summary

Implemented comprehensive enrichment pipeline for Substack drafts to restore parity between dashboard preview and live published output. Gap fixed: preview included images, subscribe CTA, and publication blurb; live article was bare HTML.

### Implementation

**Key changes to `src/dashboard/server.ts`:**
- Added `enrichSubstackBody()` async function
- Added `intersperseImages()` helper (mirrors preview distribution logic)
- Modified `saveOrUpdateSubstackDraft()` to await enrichment
- Added `resolve` import from `node:path`

**Cover image:** Uploaded to Substack CDN and prepended as `<figure>` at top  
**Inline images:** Uploaded and distributed evenly throughout body  
**Subscribe CTA:** Appended styled div from `config.leagueConfig.substackConfig.subscribeCaption`  
**Publication blurb:** Appended footer with Lab intro and engagement prompt

### Testing

**New tests added to `tests/dashboard/publish.test.ts` (4 tests):**
- Verify enriched body contains subscribe CTA and footer
- Confirm images fail gracefully if files missing
- Validate image upload workflow

**Results:** All 45 tests pass. Build passes with `npm run v2:build`.

### Architecture Decisions

1. **Image upload non-blocking:** Missing images log warning but don't block draft creation
2. **Enrichment idempotent:** Applied every draft save (create + update), ensures consistency
3. **Config-driven:** Subscribe caption and lab name from `config.leagueConfig.substackConfig`
4. **Algorithm preserved:** Inline distribution matches preview.ts logic exactly

## Outcome

Decision **Substack Payload Parity Restoration** merged to decisions.md. Feature shipped on main.

## Cross-Agent Impact

- **Data:** Payload path parity now full end-to-end  
- **DevOps:** Publish tests now validated with rich content output
