# Orchestration Log — UX

**Session:** Publish-Config Fix Coordination  
**Timestamp:** 2026-03-23T04-16-31Z  
**Agent:** UX  

## Outcome

**Status:** ✅ Decisions Merged & Implementation Ready  
**Action:** Code team to update publish-page alert copy and Config-page error handling.

## Summary

UX agent finalized two key decisions for the publish-error UX:

1. **Publisher-Substack-Config Decision:** Detect service availability on HTMX requests. Render actionable in-panel alerts instead of raw 500 errors. Distinguish "missing config" from "service unavailable despite config."

2. **Publish Missing Config Copy Decision:** Use short primary alert ("Substack publishing is not configured.") with actionable recovery details in separate hint section.

## Details

**Current State:** Stage 7 publish workflow (HTMX-based) returns HTML fragments on config errors, allowing UI to show recovery guidance instead of broken-page failures.

**New UX Behavior:**
- Alert text: "Substack publishing is not configured."
- Hint text: Links to `/config` page, names required env vars (SUBSTACK_PUBLICATION_URL, SUBSTACK_TOKEN), suggests server restart
- Applies to both missing env and service-startup failures

**Files Updated:**
- `src/dashboard/views/publish.ts` (alert rendering)
- `src/dashboard/server.ts` (error response handling)
- `src/dashboard/views/config.ts` (related config copy)

**Validation:** Focused publish and server tests passed. Full TypeScript build blocked by pre-existing `src/cli.ts` errors (unrelated).

## Related Decisions

- decisions.md: "Publisher — Substack dashboard config UX"
- decisions.md: "UX Decision Inbox — Publish Missing Config Copy"
