# Orchestration Log — DevOps Agent

**Date:** 2026-03-28T00:54:15Z  
**Agent:** DevOps  
**Topic:** Investigate current implementation state for the Lead-approved in-app read-only tool seam  
**Requester:** Backend (Squad Agent)  
**Mode:** sync  

## Outcome Summary

### Key Findings

**Existing Architecture:**
- App-owned bounded local tool loop exists in `src/agents/runner.ts` and `src/agents/local-tools.ts`
- Uses registry-derived runtime metadata from `mcp/tool-registry.mjs` with strict argument validation
- Enforces max-call cap per agent run to prevent infinite loops
- Implements allowlisted tool access (read-only NFL data + discovery tools only)

**Implementation Status:**
- ✅ In-app agent tool registry seam is complete and functional
- ✅ Allowlist enforcement prevents access to publishing/media/cache-refresh tools
- ✅ Argument validation uses Zod schemas
- ✅ Safe in-process execution without spawning external MCP server

**Missing Pieces:**
1. **Duplicate-identical-call suppression** — no detection/blocking of repeated identical tool calls in same run
2. **Negative tests** — insufficient test coverage proving blocked tools (publish_to_substack, render_table_image, refresh_nflverse_cache) stay inaccessible

**Repo Drift Found:**

| Issue | File | Severity | Details |
|-------|------|----------|---------|
| Build failure | `src/llm/providers/copilot-cli.ts` | HIGH | `verify()` method calls `exec()` with 2 args; signature expects 1 |
| Missing import | `tests/agents/runner.test.ts` | HIGH | Imports removed file `src/llm/in-app-tools.js` |
| Gateway tests pass | `tests/llm/gateway.test.ts` | OK | ✅ Passes |
| Provider tests pass | `tests/llm/provider-copilot-cli.test.ts` | OK | ✅ Passes |

### Recommendations

1. **Fix build failures** — correct `verify()` signature and remove stale import
2. **Add negative allowlist tests** — prove publishing tools throw "not allowed" errors
3. **Implement duplicate-call detection** — track tool calls per run, reject identical repeats
4. **Document in-app tool seam contract** — clarify which tools available to which agents at which stages
5. **Update system prompt guidance** — tell agents to use `local_tool_catalog` for discovery

## Files Reviewed

- `src/agents/runner.ts` — Agent orchestration and tool invocation loop
- `src/agents/local-tools.ts` — In-process tool handler bridge
- `src/llm/gateway.ts` — Provider-agnostic chat interface
- `src/llm/providers/copilot-cli.ts` — Copilot CLI provider implementation
- `mcp/tool-registry.mjs` — Source of truth for tool definitions
- `tests/agents/runner.test.ts` — Runner tests (stale import)
- `tests/llm/gateway.test.ts` — Gateway tests (passes)
- `tests/llm/provider-copilot-cli.test.ts` — Provider tests (passes)
- `package.json` — Build and test config
- `tsconfig.json` — TypeScript config

## Next Steps

→ Backend: Schedule follow-up with Code to implement duplicate-call suppression and negative test coverage  
→ Code: Fix `verify()` signature and runner test import  
→ DevOps: Re-run build validation after fixes  
