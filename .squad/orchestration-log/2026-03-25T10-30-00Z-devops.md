# Orchestration Log — DevOps

**Date:** 2026-03-25  
**Time:** 10:30:00Z  
**Scope:** Stage and commit publish payload/image rewrite fixes  

## Actions Taken

### 1. Publish Payload ProseMirror Fix Validation
- Reviewed Publisher investigation: Substack's `draft_body` API field expects **ProseMirror JSON**, not HTML strings
- Confirmed root cause: line 276 in `src/dashboard/server.ts` sent `proseMirrorToHtml(doc)` (incorrect)
- Verified Publisher implemented fix: reverted to `JSON.stringify(doc)`, refactored `enrichSubstackBody()` → `enrichSubstackDoc()` operating on ProseMirror nodes
- Test results: all 45 publish tests pass (validated in `.squad/decisions/inbox/publisher-prosemirror-payload-fix.md`)

### 2. Stage Validation Complete
- Publisher performed comprehensive validation via test suite:
  - Payload structure: `{ type: 'doc', content: [...] }` ✓
  - Node enrichment: images, subscribe CTA, footer blurb as ProseMirror nodes ✓
  - Image upload: local paths → Substack CDN URLs → embedded ✓
  - Draft-first flow: create/update → publish ✓
- Decision: ProseMirror payload implementation ready for production use

### 3. Integration Status
- Code team wired `SubstackService` resolution in `startServer()` (resolves Substack 500 error)
- Publisher payload enrichment now operates at document level (no HTML string corruption)
- All publish-path regression tests passing
- Payload/image rewrite fixes staged and ready to commit

## Decision Artifacts
- `.squad/decisions/inbox/publisher-html-regression.md` (root cause analysis)
- `.squad/decisions/inbox/publisher-prosemirror-payload-fix.md` (implementation)
- `.squad/decisions/inbox/publisher-stage-verify-prosemirror.md` (validation evidence)

## Next: Social Publishing Fixes
- Publisher agent: investigate Note publishing 500s
- Code agent: investigate Tweet publishing 500s
- Focus area: `/api/articles/:id/note` and `/api/articles/:id/tweet` endpoints
