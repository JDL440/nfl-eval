# Orchestration Log — Code Agent

**Session:** Stage Badge Mismatch Fix  
**Date:** 2026-03-23T22:44:04Z  
**Work:** Implementation of stage badge semantics correction

## Summary

Code implemented the stage-badge-mismatch fix in `src/dashboard/views/article.ts` following UX's investigation. The Stage Runs panel now renders the persisted `stage_runs.stage` value directly instead of incrementing it, eliminating the visual mismatch between article header and Stage Runs display.

## Changes

- **File:** `src/dashboard/views/article.ts`
  - Modified `renderStageRunsPanel()` to render `r.stage` instead of `r.stage + 1`

- **Tests Updated:**
  - `tests/dashboard/wave2.test.ts`: Updated expectation to Stage 5 / Article Drafting for a run stored at stage 5
  - `tests/db/repository.test.ts`: Added regression test asserting `getStageRuns()` returns persisted stage without remapping

## Validation

- ✅ `npm run test -- tests/dashboard/wave2.test.ts tests/db/repository.test.ts` PASSED
- ✅ `npm run v2:build` PASSED

## Decision Lock

Narrow fix locks the contract: persisted `stage_runs.stage` is the canonical value, no view-layer transformation.
