# Orchestration Log — Publisher Agent

**Session:** Publish Overhaul Investigation & Model Recommendation  
**Timestamp:** 2026-03-24T02:30:38Z  
**Scope:** Draft lifecycle, publishing workflow, and API integration design

## Session Summary

Publisher agent investigated Stage 7 architecture and submitted draft-first model recommendation:

1. **Current Two-Step Flow**: Stage 7 (`Publisher Pass`) is editorially ready but requires manual Substack action: open `/articles/:id/publish` → create draft → publish draft.

2. **Draft Model Recommendation**: Adopt **draft-first** on `/articles/:id/publish`:
   - **Save/Create Draft**: Create if none exists, update if exists (idempotent)  
   - **Publish Now**: Publish existing draft only; stop if no linked draft  

3. **Preview Upgrade**: Publish page preview should reuse richer preview model from `/articles/:id/preview` (title, subtitle, byline, cover, inline images, mobile viewport).

4. **Error States Clarity**: Surface draft state, Substack service config, draft save failures, URL staleness, publish failures, page refresh needs.

## Proposed API Behavior

- `POST /api/articles/:id/draft` — Idempotent save (create or update same draft)  
- `POST /api/articles/:id/publish` — Publish existing linked draft only  
- Both operations return draft URL and state for UI feedback  

## Key Technical Artifacts

- `src/dashboard/server.ts` — Draft/publish routes (lines 1277–1438)  
- `src/dashboard/views/publish.ts` — Publish page rendering  
- `src/pipeline/actions.ts` — Publisher pass logic  
- `tests/dashboard/publish.test.ts` — Regression tests  

## Status

**Draft-first model recommendation submitted to `.squad/decisions.md`.** Ready for team review and Code implementation.

**Next:** Await Coordinator implementation of idempotent draft save and publish-now sync logic.
