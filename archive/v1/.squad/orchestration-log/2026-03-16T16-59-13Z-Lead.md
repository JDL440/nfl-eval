# Orchestration Log: Lead — Tua Publish Retro

**Timestamp:** 2026-03-16T16:59:13Z  
**Agent:** Lead  
**Session:** tua-publish-retro  
**Mode:** sync

## Summary

Completed retro on Miami Tua Substack draft publish flow. Identified three process gaps:

1. **Draft URL not persisted** — Extension returns URL only in ephemeral tool response; never written to `pipeline.db` or artifacts. Recommendation: add URL persistence as hard gate in Publisher skill before Stage 7 completion.

2. **Pre-flight table check missing** — Dense markdown tables (>3 columns, >5 rows) cause Substack ProseMirror parser to choke. Neither Writer nor Editor checklists include density heuristics; only extension catches the failure at publish time.

3. **Stale "tool unavailable" escape hatch** — Publisher-pass.md template includes outdated auth-failure workaround language (lines 66-67). Now that extension works reliably, this path should be removed; agents should retry or escalate instead.

## Outcomes

- Documented all three fixes in `.squad/decisions/inbox/lead-tua-publish-retro.md`
- Recommended #1 (persist draft URL) as immediate priority for Publisher skill implementation
- Noted artifact cleanup completed: `content/articles/mia-tua-dead-cap-rebuild/publisher-pass.md` no longer contains stale tool-unavailable note

## Decisions Filed

- `lead-tua-publish-retro.md` → decisions inbox (awaiting Scribe merge)

## Next Steps

1. Approve and prioritize URL persistence fix
2. Update Publisher skill to include hard gate for substack_url persistence
3. Add table density heuristics to Publisher skill checklist
4. Remove tool-unavailable template language from publisher-pass generator
