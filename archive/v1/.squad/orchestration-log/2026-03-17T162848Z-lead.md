# Orchestration Log: Lead — Notes Card Rendering + URL Backfill

**Agent:** Lead (claude-opus-4.6 / Lead / GM Analyst)
**Timestamp:** 2026-03-17 16:28
**Trigger:** Stage review notes on nfllabstage rendered as plain links, and two Stage 8 articles lacked `substack_url` values in `content/pipeline.db`.
**Issue:** Substack cards require explicit ProseMirror `link` marks; the database missing published URLs makes it impossible to point stage reviews at prod articles once they launch.

## Task
- Backfill the missing `substack_url` entries for the two legacy Stage 8 articles.
- Rework `noteTextToProseMirror()` so inline URLs are wrapped in link marks before they reach Substack.
- Replace each broken stage review Note with a corrected version that includes the new link-marked text.

## Method
- Queried `GET https://nfllab.substack.com/api/v1/archive` for `seahawks-rb1a-target-board` and `witherspoon-extension-cap-vs-agent`, then updated their pipeline.db records with the retrieved published `/p/` URLs.
- Added the `parseNoteInline()` helper, taught `noteTextToProseMirror()` to wrap every `https://` match in a `link` mark, and confirmed the downstream `publish_note_to_substack` path still delivers valid JSON.
- Regenerated and posted the corrected stage review Notes for JSN, KC Fields, Denver, Miami, and Witherspoon, deleting the previous link-only versions once the replacements published successfully.

## Outcome
- Stage review Notes now include ProseMirror link marks and trigger Substack article cards instead of plain links, satisfying Joe’s directive.
- Pipeline DB retains durable `substack_url` values for both Stage 8 articles, so future stage/publish workflows can point to the real prod URLs.
- The durable decision and log trail now reflect the card-first presentation requirement.
