## Ralph session plan

1. Resolve squad/team context and current GitHub board state.
2. Identify all actionable open `squad:*` issues in `JDL440/nfl-eval`, excluding items already waiting on user input.
3. Move each actionable issue to `In Progress` before work starts when tooling allows.
4. Spawn the appropriate squad agents for all actionable issues in parallel as background work.
5. Collect results, summarize board status, then trigger Scribe follow-up and next Ralph round.
